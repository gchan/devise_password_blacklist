language: ruby
cache: bundler

before_install: gem update --system && gem install bundler

rvm:
  - ruby-head
  - 2.4.0
  - 2.3.3
  - 2.2.6
  - 2.1.9
  - 2.0.0

matrix:
  allow_failures:
    - rvm: ruby-head

addons:
  code_climate:
    repo_token: